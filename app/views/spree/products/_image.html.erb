<% if image %> 
  <%= image_tag image.attachment.url(:large), :itemprop => "image", data: {zoom: "/favicon.ico"} %>

<% else %>
  
  <%= large_image(@product, :itemprop => "image", data: {zoom: "/favicon.ico"}) %>

<% end %>