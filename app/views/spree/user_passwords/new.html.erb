<%= render :partial => 'spree/shared/error_messages', :locals => { :target => @spree_user } %>

<div id="forgot-password">
  <h2 class="login-title"><%= Spree.t(:forgot_password) %></h2>

  <p><%= Spree.t(:instructions_to_reset_password) %></p>
<br/>
  <%= form_for Spree::User.new, :as => :spree_user, :url => spree.reset_password_path do |f| %>
    <span class="row-fluid">
      <span class="span3"><%= f.label :email, Spree.t(:email) %></span>
      <span class="span9"><%= f.email_field :email, :class => 'span12' %></span>
    </span>
    <p>
      <%= f.submit Spree.t(:reset_password), :class => 'button primary' %>
    </p>
  <% end %>
</div>
